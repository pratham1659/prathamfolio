import React from "react";
import { ContactContainer, ContactForm, ContactInfo } from "./styles"; // Create styled components for ContactContainer, ContactForm, ContactInfo, etc.

const Contact = () => {
  return (
    <ContactContainer>
      <ContactForm>
        {/* Add contact form inputs and submit button */}
      </ContactForm>
      <ContactInfo>{/* Add contact information */}</ContactInfo>
    </ContactContainer>
  );
};

export default Contact;
